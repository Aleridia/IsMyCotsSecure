export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: "IsMyCotsSecure",
  description: "Get the most secure COTS !",
  navItems: [
    {
      label: "Home",
      href: "/",
    }
  ]
};
